class CollectionItemDTO {
  constructor(item) {
    this.id = item.id;
    this.imageUrl = item.imageUrl;
    this.price = item.price;
    this.name = item.name;
  }
}

export default CollectionItemDTO;
